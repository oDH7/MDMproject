function acess()   {
    var senha = document.getElementById("senha");
    var key={
        'key_json': ['kf53r1jj'],
    }
}
    if(key.key_json[0] == senha.value ){
        window.location.assign("index.html");
        window.location.replace("index.html");
}